{{- if .Values.caseManagementWeb.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-case-management-web
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-case-management-web
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}-case-management-web
    app.kubernetes.io/instance: {{ .Release.Name }}-case-management-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Release.Name }}-case-management-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Release.Name }}-case-management-web
    spec:
      containers:
      - image: {{ .Values.caseManagementWeb.image }}
        name: {{ .Release.Name }}-case-management-web
        env:
        - name: APPINSIGHTS_INSTRUMENTATIONKEY
          value: {{ .Values.appInsightsKey }}
        - name: IDAM_LOGIN_URL
          value: {{ .Values.idamWebUrl }}/login
        - name: CCD_GW_LOGOUT_URL
          value: https://gateway-{{ .Values.ingressHost }}/logout
        - name: CCD_API_URL
          value: https://gateway-{{ .Values.ingressHost }}/aggregated
        - name: CCD_DATA_URL
          value: https://gateway-{{ .Values.ingressHost }}/data
        - name: CCD_GW_OAUTH2_URL
          value: https://gateway-{{ .Values.ingressHost }}/oauth2
        - name: CCD_GW_OAUTH2_CLIENT_ID
          value: ccd_gateway
        - name: DM_URL
          value: https://gateway-{{ .Values.ingressHost }}/oauth2
       # - name: DM_URL_REMOTE TODO look at this
       #   value: 'false'
        - name: CCD_PAGE_SIZE
          value: '25'
        - name: POSTCODE_LOOKUP_URL
          value: https://gateway-{{ .Values.ingressHost }}/addresses?postcode=$${postcode}
        - name: PRINT_SERVICE_URL
          value: https://gateway-{{ .Values.ingressHost }}/print
       # - name: PRINT_SERVICE_URL_REMOTE TODO look at this
       #   value:
        - name: CCD_ACTIVITY_URL
          value: https://gateway-{{ .Values.ingressHost }}/activity
        - name: CCD_ACTIVITY_NEXT_POLL_REQUEST_MS
          value: '5000'
        - name: CCD_ACTIVITY_RETRY
          value: '5'
        - name: CCD_ACTIVITY_BATCH_COLLECTION_DELAY_MS
          value: '1'
        - name: CCD_ACTIVITY_MAX_REQUEST_PER_BATCH
          value: '25'
        - name: PAYMENTS_URL
          value: https://gateway-{{ .Values.ingressHost }}/payments
        {{- if .Values.caseManagementWeb.environment }}
          {{- range $key, $val := .Values.caseManagementWeb.environment }}
        - name: {{ $key }}
          value: {{ $val | quote }}
          {{- end}}
        {{- end }}
        resources:
          requests:
            memory: {{ .Values.memoryRequests }}
            cpu: {{ .Values.cpuRequests }}
          limits:
            memory: {{ .Values.memoryLimits }}
            cpu: {{ .Values.cpuLimits }}
        ports:
        - containerPort: {{ .Values.caseManagementWeb.applicationPort }}
          name: http
        livenessProbe:
          httpGet:
            path: {{ .Values.livenessPath }}
            port: {{ .Values.caseManagementWeb.applicationPort }}
          initialDelaySeconds: {{ .Values.livenessDelay }}
          timeoutSeconds: {{ .Values.livenessTimeout }}
          periodSeconds: {{ .Values.livenessPeriod }}
          failureThreshold: {{ .Values.livenessFailureThreshold }}
        readinessProbe:
          httpGet:
            path: {{ .Values.readinessPath }}
            port: {{ .Values.caseManagementWeb.applicationPort }}
          initialDelaySeconds: {{ .Values.readinessDelay }}
          timeoutSeconds: {{ .Values.readinessTimeout }}
          periodSeconds: {{ .Values.readinessPeriod }}
        imagePullPolicy: IfNotPresent
{{- end }}
