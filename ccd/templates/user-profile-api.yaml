apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-user-profile-api
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-user-profile-api
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}-user-profile-api
    app.kubernetes.io/instance: {{ .Release.Name }}-user-profile-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Release.Name }}-user-profile-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Release.Name }}-user-profile-api
    spec:
      containers:
      - image: {{ .Values.userProfileApi.image }}
        name: {{ .Release.Name }}-user-profile-api
        env:
        - name: IDAM_S2S_URL
          value: {{ .Values.s2sUrl }}
        - name: USER_PROFILE_S2S_AUTHORISED_SERVICES
          value: {{ .Values.userProfileApi.authorisedServices }}
        - name: APPINSIGHTS_INSTRUMENTATIONKEY
          value: {{ .Values.appInsightsKey }}
        - name: USER_PROFILE_DB_HOST
          value: {{ .Release.Name }}-ccd-postgres
        - name: USER_PROFILE_DB_PORT
          value: '5432'
        - name: USER_PROFILE_DB_NAME
          value: user-profile
        - name: USER_PROFILE_DB_USERNAME
          value: hmcts
        - name: USER_PROFILE_DB_PASSWORD
          value: hmcts
        - name: USER_PROFILE_DB_USE_SSL
          value: 'false'
        {{- if .Values.userProfileApi.environment }}
          {{- range $key, $val := .Values.userProfileApi.environment }}
        - name: {{ $key }}
          value: {{ $val | quote }}
          {{- end}}
        {{- end }}
        resources:
          requests:
            memory: {{ .Values.memoryRequests }}
            cpu: {{ .Values.cpuRequests }}
          limits:
            memory: {{ .Values.memoryLimits }}
            cpu: {{ .Values.cpuLimits }}
        ports:
        - containerPort: {{ .Values.userProfileApi.applicationPort }}
          name: http
        livenessProbe:
          httpGet:
            path: {{ .Values.livenessPath }}
            port: {{ .Values.userProfileApi.applicationPort }}
          initialDelaySeconds: {{ .Values.livenessDelay }}
          timeoutSeconds: {{ .Values.livenessTimeout }}
          periodSeconds: {{ .Values.livenessPeriod }}
          failureThreshold: {{ .Values.livenessFailureThreshold }}
        readinessProbe:
          httpGet:
            path: {{ .Values.readinessPath }}
            port: {{ .Values.userProfileApi.applicationPort }}
          initialDelaySeconds: {{ .Values.readinessDelay }}
          timeoutSeconds: {{ .Values.readinessTimeout }}
          periodSeconds: {{ .Values.readinessPeriod }}
        imagePullPolicy: IfNotPresent
